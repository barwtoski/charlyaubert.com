---
import Layout from '../../layouts/Layout.astro';

const articles = [
  {
    title: "Transformer sa relation a l'argent — vivre la vie de son choix",
    description: "De la prise de conscience a l'independance financiere. Protocole concret, levier bancaire, interets composes, et pourquoi la frugalite est un superpouvoir.",
    date: "18 fevrier 2026",
    slug: "argent",
    tags: ["argent", "independance", "strategie"]
  },
  {
    title: "NILA Blueprint — Construis ton copilote de vie",
    description: "Guide complet pour installer un systeme de pilotage de vie avec une IA. Obsidian, PARA, elans, decharge vocale. Gratuit et open source.",
    date: "8 fevrier 2026",
    slug: "nila",
    tags: ["ia", "organisation", "open-source"]
  }
];
---

<Layout title="Blog — Charly Aubert" description="Articles sur la construction, l'IA, et la vie a LAOM.">
  <article class="prose">
    <h1>Ce que j'ecris</h1>
    <span class="update-date">2 articles</span>

    <div class="blog-list">
      {articles.map((article) => (
        <a href={`/blog/${article.slug}`} class="blog-item">
          <div class="blog-item-content">
            <span class="blog-date">{article.date}</span>
            <h2 class="blog-title">{article.title}</h2>
            <p class="blog-desc">{article.description}</p>
            <div class="blog-tags">
              {article.tags.map((tag) => (
                <span class="blog-tag">{tag}</span>
              ))}
            </div>
          </div>
          <span class="blog-arrow">-&gt;</span>
        </a>
      ))}
    </div>
  </article>
</Layout>

<style>
.blog-list {
  display: flex;
  flex-direction: column;
  gap: 0;
  margin-top: var(--space-lg);
}

.blog-item {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 1rem;
  padding: 1.5rem 1rem;
  border-bottom: 1px solid var(--color-border);
  text-decoration: none;
  color: var(--color-text-sub);
  transition: background-color 0.2s ease, color 0.2s ease;
  border-radius: var(--radius-sm);
  margin: 0 -1rem;
}

.blog-item:first-child {
  border-top: 1px solid var(--color-border);
}

.blog-item:hover {
  background-color: var(--color-green-dim);
  color: var(--color-text);
}

.blog-item-content {
  flex: 1;
}

.blog-date {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--color-text-muted);
}

.blog-item h2.blog-title {
  font-family: var(--font-sans);
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--color-text);
  margin: 0.25rem 0 0.4rem;
  line-height: 1.4;
}

.blog-desc {
  font-size: 0.9rem;
  color: var(--color-text-muted);
  line-height: 1.6;
  margin: 0;
}

.blog-tags {
  display: flex;
  gap: 0.5rem;
  margin-top: 0.5rem;
  flex-wrap: wrap;
}

.blog-tag {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  color: var(--color-orange);
  background: rgba(250, 179, 135, 0.1);
  padding: 0.15rem 0.5rem;
  border-radius: 4px;
}

.blog-arrow {
  font-family: var(--font-mono);
  font-size: 0.85rem;
  color: var(--color-green);
  flex-shrink: 0;
  opacity: 0;
  margin-top: 1.5rem;
  transition: opacity 0.2s ease;
}

.blog-item:hover .blog-arrow {
  opacity: 1;
}
</style>
